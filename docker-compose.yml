version: "3.9"

services:
  gpt-api:
    build: .
    container_name: gpt_api_container
    ports:
      - "8000:8000"
    environment:
      OPENAI_API_KEY: your_openai_api_key_here
      OPENAI_MODEL: gpt-4o-mini
    restart: always
